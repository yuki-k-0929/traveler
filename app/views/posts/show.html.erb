<h2>旅行先</h2>
<p><%= @post.location %></p>
<h2>感想</h2>
<p><%= @post.text %></p>
<h2>画像</h2>
<%= attachment_image_tag @post, :image, format: "ipeg", size: "300x300" %><br>
<%= link_to "編集する", edit_post_path(@post.id) %><br>
<%= link_to "削除する", post_path(@post.id), data: { turbo_method: :delete, turbo_confirm: "本当に消しますか？"} %><br>
<%= link_to "投稿一覧ページへ", posts_path %>